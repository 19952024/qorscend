/** @type {import('next').NextConfig} */
const nextConfig = {
  typescript: {
    ignoreBuildErrors: true,
  },
  images: {
    unoptimized: true,
  },
  // API routes are now handled directly by Next.js, no rewrites needed
}

export default nextConfig
